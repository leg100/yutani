---
:backends:
  - yaml

:hierarchy:
  - '%{stack_name}/%{env}/%{region}'
  - '%{stack_name}/%{env}/%{module_name}'
  - '%{stack_name}/%{env}/common'
  - '%{stack_name}/common'
  - common

:yaml:
  :datadir: ./example/hieradata

:logger: noop
